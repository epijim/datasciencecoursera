".csv",
sep = ""))
data <- c(data, temp[[pollutant]])
}
data <- numeric()
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
data <- c(data, temp[[pollutant]])
}
data <- numeric()
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- complete.cases(temp)
}
complete <- sum(complete.cases(temp))
complete <- as.integer(sum(complete.cases(temp)))
complete <- c(id,sum(complete.cases(temp)))
length(id)
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
# id is integer
id <- as.integer(id)
# directory and pollutant are character
directory <- as.character(directory)
df <- data.frame(matrix(vector(),length(id),2),
dimnames=list(c(),c("id","nobs")),
stringsAsFactors=F)
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
result <- rbind(df,complete)
}
return(result)
}
complete("specdata",1)
df <- data.frame(matrix(vector(),length(id),2),
dimnames=list(c(),c("id","nobs")),
stringsAsFactors=F)
df <- data.frame(length(id),2)
View(df)
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
# id is integer
id <- as.integer(id)
# directory and pollutant are character
directory <- as.character(directory)
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
result <- rbind(df,complete)
}
return(result)
}
complete("specdata",1)
results <- data.frame(id=rep(NA,length(id))
nobs=rep(0,length(id))
)
results <- data.frame(id=rep(NA,length(id))
nobs=rep(0,length(id))
)
results <- data.frame(id=rep(NA,length(id))
nobs=rep(0,length(id))
length(id)
nrows <- length(id)
results <- data.frame(id=rep(NA,nrows)
nobs=rep(0,nrows)
results <- data.frame(id=rep(NA,nrows),
nobs=rep(0,nrows)
)
df <- data.frame(id=rep(NA,nrows),
nobs=rep(0,nrows)
)
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
result <- rbind(df,complete)
}
View(result)
df <- data.frame(id=numeric(0),
nobs=numeric(0)
)
df <- data.frame(id=numeric(0),
nobs=numeric(0)
)
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
result <- rbind(df,complete)
}
df <- data.frame(id=NA,
nobs=NA
)
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
result <- rbind(df,complete)
}
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
result <- rbind(df,complete)
}
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
df[nrow(df)+1,] <- complete
}
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
# id is integer
id <- as.integer(id)
# directory and pollutant are character
directory <- as.character(directory)
nrows <- length(id)
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
df[nrow(df)+1,] <- complete
}
return(df)
}
complete("specdata",1)
complete("specdata",c(2,4,8,10,12))
complete <- c(4,3)
df[nrow(df)+1,] <- complete
df
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
# id is integer
id <- as.integer(id)
# directory and pollutant are character
directory <- as.character(directory)
nrows <- length(id)
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
df[nrow(df)+1,] <- complete
}
return(df)
}
complete("specdata")
complete("specdata",1)
complete("specdata",2)
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
df[nrow(df)+1,] <- complete
complete <- c(4,3)
df[nrow(df)+1,] <- complete
complete <- c(4,6)
df[nrow(df)+1,] <- complete
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
# id is integer
id <- as.integer(id)
# directory and pollutant are character
directory <- as.character(directory)
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
df[nrow(df)+1,] <- complete
}
return(df)
}
complete("specdata",1)
complete("specdata",c(1,2))
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
# directory and pollutant are character
directory <- as.character(directory)
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
df[nrow(df)+1,] <- complete
}
return(df)
}
complete("specdata",c(1,2))
id <- c(1,2)
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
df[nrow(df)+1,] <- complete
}
30:25
complete("specdata",3
)
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
# directory and pollutant are character
directory <- as.character(directory)
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
df[nrow(df)+1,] <- complete
}
return(df)
}
complete("specdata",3)
for (i in id){
print(i)
}
id <- c(2,4,6)
for (i in id){
print(i)
}
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
# directory and pollutant are character
directory <- as.character(directory)
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- c(id,sum(complete.cases(temp)))
df[nrow(df)+1,] <- complete
}
return(df)
}
complete("specdata", 30:25)
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
30:25
complete("specdata", 30:25)
complete <- c(id,sum(complete.cases(temp)))
complete <- NULL
complete <- c(id,sum(complete.cases(temp)))
complete.cases(temp)
sum(complete.cases(temp))
complete <- function(directory, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return a data frame of the form:
## id nobs
## 1  117
## 2  1041
## ...
## where 'id' is the monitor ID number and 'nobs' is the
## number of complete cases
# directory and pollutant are character
directory <- as.character(directory)
df <- data.frame(id=NA,
nobs=NA
)[numeric(0),]
for (i in id) {
temp <- read.csv(paste(directory,
"/",
formatC(i,
width = 3,
flag = "0"),
".csv",
sep = ""))
complete <- NULL
complete <- c(i,sum(complete.cases(temp)))
df[nrow(df)+1,] <- complete
}
return(df)
}
complete("specdata", 30:25)
complete("specdata", 1)
complete("specdata", c(2,4,8,10,12))
submit()
submit()
submit()
corr <- function(directory, threshold = 0) {
files <- list.files( path = directory )
cr <- c()
for(f in 1:length(files)){
data <- read.csv( paste(directory, "/", files[f], sep="") )
data <- data[complete.cases(data),]
if ( nrow(data) > threshold ) {
cr <- c(cr, cor(data$sulfate, data$nitrate) ) # append corralations
}
}
return( cr )
}
cr <- corr("specdata", 150)
head(cr)
summary(cr)
cr <- corr("specdata", 400)
head(cr)
summary(cr)
cr <- corr("specdata")
summary(cr)
submit()
submit()
submit()
